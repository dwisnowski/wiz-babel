{
  "name": "wiz-babel",
  "version": "0.0.0",
  "description": "An application to babel with your baby's and toddlers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile"
  },
  "keywords": [
    "babel"
  ],
  "author": "David Wisnowski",
  "license": "MIT",
  "dependencies": {
    "alexa-sdk": "^1.0.6"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
